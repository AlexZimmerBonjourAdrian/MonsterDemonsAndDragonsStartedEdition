using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;
using DNECore;
namespace DNECore {

    public class CDialogNode : CNode
    {
        /*
        public ConnectionPoint inPoint;
        public List<ConnectionPoint> outPoints = new List<ConnectionPoint>();
        public List<string> triggers = new List<string>();

        public string title;
        public string text;

        private float offset;
        private float button_height = EditorGUIUtility.singleLineHeight + EditorGUIUtility.standardVerticalSpacing + 4f;
        private bool isAddClicked = false;
        private bool isRemoveClicked = false;
        private Vector2 scroll;

        public CDialogNode(CDialogueNodeEditor editor, Vector2 position) : base(editor, position)
        {
            Init(position);
        }
        public CDialogNode(CDialogueNodeEditor editor, NodeInfo info) : base(editor,info) { Init(new Vector2(info.rect.x, info.rect.y));
            Init(new Vector2(info.rect.x, info.rect.y));
            
        // Start is called before the first frame update

    }
        public override void Init(Vector2 position)
        {
            width = 300;
            height = 200;
            rect = new Rect(position.x, position.y, width, height);
            inPoint = new CConnectionPoint(this, ConnectionPointType.In, editor.)
        }
    */
    }
    
}
